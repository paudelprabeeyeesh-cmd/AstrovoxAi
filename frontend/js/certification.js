document.addEventListener('DOMContentLoaded', () => {
  const section = document.getElementById('certification');
  if (section) {
    section.innerHTML = '<h2>v1.0 Certified</h2><p>All quality gates passed.</p>';
  }
});
